/// The .riv file path for the working Android project.
const rivPathAndroid = 'android/app/src/main/res/raw/input.riv';

/// The .riv file path for the working iOS project.
const rivPathIOS = 'ios/input.riv';

/// The .riv file path for the working Flutter project.
const rivPathFlutter = 'flutter/assets/input.riv';

/// The path to the Android template project.
const androidTemplatePath = 'android';

/// The path to the iOS template project.
const iosTemplatePath = 'ios';

/// The path to the Flutter template project.
const flutterTemplatePath = 'flutter';

/// The path to the Android working project.
const androidWorkingPath = 'android';

/// The path to the iOS working project.
const iosWorkingPath = 'ios';

/// The path to the Flutter working project.
const flutterWorkingPath = 'flutter';
